---
// CSS
import '../css/auth/newPassword.css'

// AUTH
import { isLoggedIn } from '../utils/auth';

// COMPONENTS
import Layout from '../layouts/Layout.astro';
import FormNewPassword from '../components/auth/FormNewPassword.jsx';

// REDIRECT
const isAuth = Astro.cookies.get("AUTH_TOKEN");
if (isLoggedIn(isAuth)) {
	return Astro.redirect("/")
}
---

<Layout title="Reestablecer Contraseña - SSAA2">
	<main class="newPassword">
		<FormNewPassword client:only="react"/>
	</main>
</Layout>
